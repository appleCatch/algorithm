不通过使用加减乘除运算，实现加法
问题分析：
如果不是用四则运算，那只有使用位运算了，通过将数字转换为二进制，分析两个数相加的实现。
1、相同的位相加时，出现0|1，1|0时结果均为1， 出现0|0，1|1时，结果为0， 和位运算中的异或操作是一致的。
2、相加解决了，现在需要考虑的就是进位的问题，可以通过两个二进制数的与运算配合左移位操作实现。
3、将以上的两个结果继续重复完成相加的动作，直到没有进位出现为止。
4、根据递归算法设计程序。