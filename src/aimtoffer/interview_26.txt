复杂链表复制：
链表中每个节点除了有一个next节点外，还有一个sibling节点，指向链表中的任意节点或NULL

思路，根据pNext可以很容易的复制整个链表
难点在于，sibling节点的指针是没有规律的，既可以指向前面的节点，也可以指向后面的节点。

所以在根据next复制链表的时候可以建立对应的映射表，这样在复制sibling指针的时候可以从映射表里直接获取旧节点对应的新节点。

针对本题还有一种更好的方法，复制的新节点放在就节点的后面，组成类似A->A'->B->B'->C->C'->D->D'
这样，sibling指针就有了规律，即统一向后移动一个指针。